# -----------------------------
# Supabase Environment Variables
# -----------------------------

# VITE_SUPABASE_URL
# The unique URL for your Supabase project's API endpoint.
# Find it in your Supabase Dashboard under Project Settings > API > Project URL.
# Used by the frontend to connect to your Supabase backend.
VITE_SUPABASE_URL=

# SUPABASE_PUBLISHABLE_KEY
# The public (anon) API key for your Supabase project. Safe for client-side use.
# Find it in Project Settings > API > Project API keys > anon public.
# Used by the frontend to make requests to Supabase with limited permissions.
SUPABASE_PUBLISHABLE_KEY=

# SUPABASE_SERVICE_ROLE_KEY
# The secret service role key for your Supabase project. Has elevated privileges.
# Find it in Project Settings > API > Project API keys > service_role.
# Use ONLY in secure server-side environments. NEVER expose in client-side code or public repos.
SUPABASE_SERVICE_ROLE_KEY=